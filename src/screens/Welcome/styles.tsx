import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textFooter: {
    fontSize: 16,
    fontWeight: 'bold',
    color: "#808080"
  },
  textBody: {
    fontSize: 18,
    fontWeight: 'bold',
  },

  divider: {
    padding: 30
  },
});

export default styles;